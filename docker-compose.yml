version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_DATABASE_URL=sqlite:///./app.db
    volumes:
      - app_data:/app/data

volumes:
  app_data:
